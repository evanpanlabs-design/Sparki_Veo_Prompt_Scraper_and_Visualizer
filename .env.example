# MiniMax API credentials — copy from your MiniMax dashboard
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_API_BASE=https://api.minimaxi.com/v1
LLM_MODEL=MiniMax-M2.7

# GitHub token for --publish (generate at github.com/settings/tokens)
# Needs: repo scope (for push access to GitHub Pages)
GH_TOKEN=your-gh-token-here
